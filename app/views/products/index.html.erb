<p id="notice" class="padding"><%= notice %></p>

<h3 class="headline_products">Our scooters for your mission</h3>

<div class="container-fluid">
  <div class="row">
    <% cache(cache_key_for_products) do %>
      <%= render @products %>
    <% end %>
  </div>
</div>
<% Product.order("created_at DESC")%>
<%= link_to 'New Product', new_product_path if user_signed_in? && @user.admin %>  
